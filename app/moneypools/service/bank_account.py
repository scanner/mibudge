"""
BankAccount service -- Phase 5.

Planned operations:
    create(...)  -- create account, add owners, create Unallocated budget,
                    back-link unallocated_budget_id.
    rename(account, name)
    delete(account)
"""
