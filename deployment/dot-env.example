# General
# ------------------------------------------------------------------------------
DJANGO_SETTINGS_MODULE=config.settings
ADMINISTRATIVE_EMAIL_ADDRESS=admin@example.net
DJANGO_SECRET_KEY=changeme-generate-a-real-secret-key
DJANGO_ADMIN_URL=changeme-random-admin-path/
DJANGO_ALLOWED_HOSTS=.example.com

# Security
# ------------------------------------------------------------------------------
DJANGO_SECURE_SSL_REDIRECT=True

# Email
# ------------------------------------------------------------------------------
# DJANGO_EMAIL_BACKEND defaults to django.core.mail.backends.smtp.EmailBackend.
# Supply an authenticated SMTP relay in production.  For local dev, point at
# Mailpit instead (see dot-env.dev / dot-env.docker-dev).
#
# To switch to django-anymail (the package is already installed), set
# DJANGO_EMAIL_BACKEND to the appropriate anymail backend, e.g.:
#   DJANGO_EMAIL_BACKEND=anymail.backends.postmark.EmailBackend
# and add anymail to INSTALLED_APPS and configure its provider settings.
DJANGO_DEFAULT_FROM_EMAIL=My Budgets <noreply@example.com>
DJANGO_SERVER_EMAIL=noreply@example.com
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=changeme
EMAIL_HOST_PASSWORD=changeme
EMAIL_USE_TLS=True

# django-allauth
# ------------------------------------------------------------------------------
DJANGO_ACCOUNT_ALLOW_REGISTRATION=True

# Gunicorn
# ------------------------------------------------------------------------------
WEB_CONCURRENCY=4

# Sentry
# ------------------------------------------------------------------------------
SENTRY_DSN=

# Redis
# ------------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# Celery
# ------------------------------------------------------------------------------
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_FLOWER_USER=changeme
CELERY_FLOWER_PASSWORD=changeme

# PostgreSQL
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=mibudge
POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgres://changeme:changeme@postgres:5432/mibudge

# django-fernet-encrypted-fields
# ------------------------------------------------------------------------------
# Required. Salt used to derive the encryption key for sensitive fields stored
# at rest (e.g. BankAccount.account_number). The key is derived from
# SECRET_KEY + SALT_KEY via PBKDF2-SHA256.
#
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
#
# Key rotation: set to a comma-separated list of salts. The first value
# encrypts new data; the rest decrypt existing records. Once all records have
# been re-saved, remove the old value.
SALT_KEY=changeme-generate-with-openssl-rand-hex-32
