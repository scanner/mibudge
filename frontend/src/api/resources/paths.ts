//
// Base paths of the REST API.  API layer.
//
// Versioned resources live under `/api/v1/`; the JWT endpoints stay at
// `/api/token/...` across API versions.
//

export const V1 = "/api/v1";
export const AUTH = "/api";
